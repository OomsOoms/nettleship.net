const testService = require('./test.service');
const userService = require('./user.service');
const authService = require('./auth.service');
const gameSessionService = require('./game.service');

module.exports = {
    testService,
    userService,
    authService,
    gameSessionService,
};